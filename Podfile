# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Food Track' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Food Track

  target 'Food TrackTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Food TrackUITests' do
    # Pods for testing
  end

  # Parse pods
  pod 'Parse'
  pod 'AlamofireImage'
end
